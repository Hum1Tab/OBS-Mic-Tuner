# v0.2.0 検証記録

## v1.0.0（2026-09-10）

GitHub正式リリースの更新検知を追加。18件の単体テストが通過しました。新版・同一版・旧版・プレリリース・下書き・EXE欠落・外部URL・オフライン・復帰・同時要求を検証。Electron実画面では新版でボタン表示とダウンロードURL、同一版とオフラインで非表示を確認しました。配布用app.asarでも更新ボタンと全録音操作テストが通過しています。

画面内ブランドとfaviconはWindowsアイコンと同じ `assets/icon.png` を参照します。ICOはそのPNGを格納した形式です。画面記録は `test-output/v1-update.png` と `v1-*.png`。GitHubの公開配布先と実リリースの確認は公開範囲の決定後に行います。

## v0.3.1 文言整理（2026-09-10）

常設のプライバシー表示、パンくず、重複する説明とフィルタの定型解説を削除。録音状態、操作結果、測定上の注意は維持しました。詳しい案内はヘルプに集約しています。

`npm test` 全16件と配布用app.asarの操作テストが通過。録音4区間、A/B再生、シーク、コピー・保存、再計算、削除、中止、権限拒否、切断を確認しました。測定・試聴・設定画面の画像を確認し、900×720でも横はみ出しはありません。画面記録は `test-output/v031-*.png`。音声処理モデルの変更はありません。

## v0.3.0 全体改修（2026-09-10）

計算モデルを維持し、UIと操作を全面改修。3画面ナビゲーション、実波形表示、独自プレイヤー、シーク、左右キーのA/B切替、ヘルプ、900×720のウィンドウを追加検証します。`npm test` 16件は再通過。新しい操作テストでは、初期ナビゲーションの無効化、ヘルプの開閉、実再生、シーク値、A/B継続再生、画面遷移、最小幅の横はみ出しを確認します。

画面の記録: `test-output/v03-measure.png`、`v03-recording.png`、`v03-listen.png`、`v03-export.png`、`v03-compact.png`。

2026-09-10、配布用app.asarで全操作テストが通過しました。測定・試聴・設定の全画面を900×720で横はみ出しなしと確認し、`v03-measure-compact.png`、`v03-listen-compact.png`も記録しました。既定サイズと最小サイズの画面を目視確認済みです。

以下は引き続き使用している音声モデルの検証履歴です。

## 数値モデル

対象: OBS Studio 32.2.2、48 kHz、モノラル。コンプレッサーは振幅包絡とアタック/リリース、エキスパンダーはRMS検出とdB領域のゲイン平滑化、リミッターはOBSの0.001 msアタックを数値モデル化しています。

前回の静的ピーク式は廃止しました。候補は実波形に適用し、処理後のレベルから選択します。評価重み・声量目標・候補集合は製品の設計値であり、学術的に万人の最適音質を証明するものではありません。

## OBS本体との独立比較

インストール済みOBS 32.2.2の `obs.dll` と `obs-filters.dll` を独立プロセスでロードし、合成入力ソースに2秒のテスト波形を渡しました。マイクやユーザーのシーン設定は使用していません。入力は無音、インパルス、強弱のある正弦波を含みます。

| 対象 | 最大サンプル絶対誤差 | 誤差RMS |
|---|---:|---:|
| コンプレッサー | 0.000004292 | 0.000000342 |
| エキスパンダー | 0.000047684 | 0.000001360 |
| リミッター | 0.000001073 | 0.000000139 |
| 3段チェーン | 0.000057310 | 0.000001662 |

すべて最大絶対誤差0.0002未満という検証条件を満たしました。数値は浮動小数点振幅です。この特定波形・設定での比較であり、すべての設定・OBS版・音声を保証するものではありません。

再現: `node scripts/native-cases.mjs`、`python scripts/native-obs.py`。結果は `test-output/native/comparison.json`。

## 自動テスト

`npm test` の16件で、以下を検証します。

- 入力ゲイン変更に対するノイズ抑制の案内の一貫性
- 調整後声量、ピーク、A/BのRMS一致、小声保護
- 入力不足、NaN、発話不足、無言区間への発話混入、クリップの拒否
- ノイズと小声が分離できない場合のエキスパンダー無効化
- RNNoise処理後の波形を使った再計算
- 実際のWASMによる無音処理と記録ごとの状態初期化
- 発話確率だけでは通過する一定音を、音量変化の検査で拒否
- 瞬間ピーク、定常入力、リリース挙動、無音、WAV出力

## UI操作テスト

Windowsの読み上げ合成音を仮想マイク入力に使います。AudioWorkletで4区間を実際に取得し、RNNoiseの発話検出と候補評価を実行します。試聴ファイルの長さ、A/B切替、コピー、保存、RNNoise切替、仕上がり変更、音声削除、中止、権限拒否、トラック切断をチェックします。

テスト結果画面は `test-output/result-v02.png` と `test-output/result-v02-bottom.png`。

2026-09-09に開発版と、配布用 `app.asar` に格納したコード・リソースの両方で一連の操作テストが通過しました。テストはElectron 44.2.0で実行。これは実マイクの音質評価や別PCでの動作確認とは区別しています。

## 残る評価範囲

- 複数の実マイク・実話者・実部屋での主観的音質評価
- クリーンなWindows PCや異なるオーディオドライバーでの確認
- RNNoiseのOBS本体との一致（同梱WASMのモデルは別である可能性がある）
- 機器/OS独自の加工、EQ、残響、True Peak、LUFS、配信先エンコード

## 参照資料

- [OBS compressor 32.2.2](https://github.com/obsproject/obs-studio/blob/32.2.2/plugins/obs-filters/compressor-filter.c)
- [OBS expander 32.2.2](https://github.com/obsproject/obs-studio/blob/32.2.2/plugins/obs-filters/expander-filter.c)
- [OBS limiter 32.2.2](https://github.com/obsproject/obs-studio/blob/32.2.2/plugins/obs-filters/limiter-filter.c)
- [RNNoise WASM 2025.1.5](https://github.com/shiguredo/rnnoise-wasm/tree/2025.1.5)

v0.1.0のレビューは `THEORY-REVIEW.md` に履歴として残しています。
