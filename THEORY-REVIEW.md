# OBS Mic Tuner v0.1.0 理論レビュー

> 履歴資料です。v0.2.0ではPCM時間応答・小声測定・発話検出・A/B試聴へ改修しました。現在の検証結果は `VALIDATION.md` を参照してください。以下の旧版の計算式は現行アプリでは使用していません。

確認日: 2026-09-09。対象は現行の計算式とOBS公式資料・ソースコード。実マイクやOBSを動かした音質比較ではありません。

## 判定

「声量と環境音を測定し、OBSの設定初期値を提案する」という構想は成立します。ただし現行版は暫定的なルールベースの設定提案であり、最適化や音質改善を実証したものではありません。前回のテスト通過はソフトウェアの動作確認であり、提案の音響的な妥当性の証明にはなりません。

## 妥当な基礎

- 約20msのRMSとサンプルピークは短時間の声量・入力余裕を調べる基本的な指標として適切。ただしRMS中央値はLUFSや聴感音量そのものではありません。
- 無言・普通の声・大きな声を分けて収録する設計は、環境音と声量差を調べる上で合理的です。25秒は簡易測定の長さであり、十分な精度を保証する根拠はまだありません。
- ノイズ抑制（必要時）→コンプレッサー→エキスパンダー→リミッターという構成は、OBSの公式説明と矛盾しません。これだけが正解という意味ではありません。
- ノイズから離れ、弱い声より低いところにエキスパンダーのしきい値を置く方針は妥当です。ただし両条件を同時に満たせないときの処理が必要です。

## 販売品質にするための重要な修正点

### 1. コンプレッサーの出力予測に時間応答がない

`app/analysis.mjs:20–21` は、静的な入出力曲線に最大サンプルピークを代入しています。OBSではサンプル絶対値にアタック・リリースの平滑化をかけた包絡で圧縮量を決めます。最大値だけから実際の圧縮後ピークを求めることはできません。

数値例: 普段の声 -28 dBFS、しきい値 -21 dB、比率2.5:1、ゲイン+8 dB。ピーク -3 dBFSに対する現在の予測は -5.8 dBFS。長い無音直後の単一サンプルをOBSの包絡式で評価すると、48kHz・6msアタックでは最初のサンプルは圧縮されず、リミッター前は +5 dBFSになります。差は10.8 dBです。

これは数式による極端な反例であり、実際のOBS録音結果ではありません。後段リミッターがあるため、この値をそのまま最終出力の音割れと解釈してはいけません。ただしリミッターが担う減衰を過小評価していることは示します。普段の声への圧縮やリリース中のゲイン低下も未計算です。

対策: 生PCMに対して対象OBSバージョン相当の時間応答を計算し、処理後の声量・ピーク・ゲインリダクションを測る。最終段も含めて評価し、単純な静的ピーク式だけで出力ゲインを決めない。

### 2. ノイズ抑制の要否が入力ゲインに左右される

`app/analysis.mjs:30` は入力環境音が -55 dBFSを超えるかだけで判定します。

再現例: 環境音 -65 / 声 -35 dBFSでは出力ゲイン+15 dB、抑制は無効。同じ信号を15 dB上げた環境音 -50 / 声 -20 dBFSではゲイン0 dB、抑制は有効。声とノイズの差も、ゲイン補正後の環境音 -50 dBFSも同じなのに提案が反転します。

対策: 目標声量へ補正した後のノイズ、声とのレベル差、定常性、周波数的特徴、および試聴を組み合わせる。RNNoiseは強い騒音を万能に除去できるものではなく、抑制による声の変化も評価する。

### 3. 音声判定と語尾の保護が不十分

`app/analysis.mjs:12–15` は音量だけで発話区間を選びます。音楽や連続した非音声の信号も、無言区間より大きければ声として通過します。またノイズ+8 dB以下の弱い発声は最初から除外されるため、その後の下位15%値で語尾の保護を保証できません。

対策: 発話検出、最低発話時間、明示的な小声測定、語尾の試聴を導入する。音量基準だけで続ける場合は「声検出」とは区別し、推定の確かさを表示する。

### 4. エキスパンダー入力の推定が不完全

`app/analysis.mjs:22` は未加工のノイズと弱い声に出力ゲインを足すだけです。前段RNNoiseによるレベル変化とコンプレッサーの動的な減衰を含みません。近似として利用できる条件はありますが、実際の後段入力を保証できません。声とノイズが近いときも常に有効を提案しています。

対策: 前段処理後に測り直す。ノイズを抑える条件と弱い声を保護する条件が両立しない場合は、エキスパンダーを無効候補にして環境改善や再測定を案内する。

### 5. 固定値に最適化の裏付けがない

声量目標 -20 dBFS（短時間RMS中央値）、しきい値=声量+7 dB、比率2.5/4、各時間定数は本ツールの暫定値です。OBS公式の推奨最適値ではありません。声質、リズム、マイクの周波数特性、残響は評価していないため、「あなたのベストな音質」とは言えません。

対策: 「声量差をどれだけ抑えるか」「語尾をどれだけ残すか」など評価基準を定義する。候補を実処理して比較し、同じ聴感音量でA/B試聴して好みを選べるようにする。マイク・話者・部屋を変えた音声で検証する。

## 測定経路の確認

ブラウザのエコー除去・ノイズ抑制・自動ゲインをfalseで要求する方針は適切ですが、現状は `getSettings()` で採用された設定を確認していません。同じマイクでもOSや機器側の加工、チャンネル処理などによってOBSの入力と一致しない可能性があるため、OBSでの入力レベル比較が必要です。クリップ判定も「0.999以上のサンプル数」による兆候であり、低いレベルで発生したアナログ歪みなどを検出するものではありません。

## 推奨する製品の到達点

無言・小声・普通・大声の測定 → 測定品質を判定 → 対象OBS版に合わせた処理で候補を評価 → 加工前後を音量をそろえて試聴 → 設定を出力 → OBSで最終確認。

「ベストを自動断定」ではなく、「測定と試聴で、自分に合うOBSマイク設定を見つける」という価値なら、技術的な説明と実際の提供内容を一致させられます。

## 再現と資料

旧版の数値例はこの文書に記録しています。現在の `node scripts/theory-audit.mjs` はv0.2.0の回帰確認に更新済みです。このレビュー時点ではアプリの計算式やEXEは変更していませんでしたが、その後v0.2.0へ改修しました。

- [OBS Compressorソース](https://raw.githubusercontent.com/obsproject/obs-studio/master/plugins/obs-filters/compressor-filter.c): analyze_envelope / process_compression。確認時点のmasterを参照。販売版では対象バージョンを固定して照合する必要があります。
- [OBS Expander](https://obsproject.com/kb/expander-filter): 検出方式・しきい値・配置。
- [OBS Limiter](https://obsproject.com/kb/limiter-filter): 最終段への配置。
- [OBS Noise Suppression](https://obsproject.com/kb/noise-suppression-filter): 適用範囲と制限。
- [W3C Media Capture and Streams](https://www.w3.org/TR/mediacapture-streams/): 制約要求と実際のトラック設定。
